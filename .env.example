# Required at build time. The build fails loudly if this is unset (src/config.ts).
# WhatsApp business number, international format, digits only:
#   no '+', no spaces, no leading '0'. Example: 6281234567890
WHATSAPP_NUMBER=
